{% extends 'base.html.twig' %}

{% block header_title %}
    <h1>Organisez enfin votre travail et votre vie!</h1>
    <h2>Gagnez en concentration, organisation et sérénité avec ToDo App. 
    La 1ère application de gestion de tâches et de to do list.</h2>
{% endblock %}

{% block body %}

    <div class="row">
    {% if app.user %}
                    {% if not is_granted('ROLE_VIEWER') %}
        <a href="{{ path('task_create') }}" class="btn btn-success">Créer une nouvelle tâche</a>
                    {% endif %}
                {% endif %}
        <a href="{{ path('task_list') }}" class="btn btn-info">Consulter la liste des tâches à faire</a>
        
    </div>
{% endblock %}
