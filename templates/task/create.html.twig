{% extends 'base2.html.twig' %}

{% block body %}
    <div class="row">
        {{ form_start(form, {'action' : path('task_create')}) }}
            {{ form_widget(form) }}

            <button type="submit" class="btn btn-success pull-right" style="margin-top: 24px">Ajouter</button>
        {{ form_end(form) }}

        <a href="{{ path('task_list') }}" class="btn btn-primary" style="margin-top: 24px">Retour à la liste des tâches</a>
    </div>
{% endblock %}
